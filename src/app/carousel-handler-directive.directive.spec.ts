import { CarouselHandlerDirectiveDirective } from './carousel-handler-directive.directive';

describe('CarouselHandlerDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CarouselHandlerDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
