import { SpacePipePipe } from './space-pipe.pipe';

describe('SpacePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SpacePipePipe();
    expect(pipe).toBeTruthy();
  });
});
